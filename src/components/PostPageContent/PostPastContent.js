import React from 'react';
import styles from './PostPageContent.module.css';

export default (props) => 
    <main style={{width:760, display:'flex', flexDirection:'column'}}>
        {children}
    </main> 